# 1.as sidebar menu  within ouur app
"""with st.sidebar:
    selected = option_menu(
        menu_title="Main Menu", #this only shows text wriitten above navigation-bar Top.
        options=["Home","Fodd Items","Contact Us ","Payment"], #required
        icons=["house-gear","basket-fill","bug-fill","cash-coin"],  #only write name of icons from bootstrap https://icons.getbootstrap.com/. 
        menu_icon="cast",
        default_index=0, #both are optional.
    )

if selected=="Home":
    st.title(f"You have selected {selected}")
if selected=="Food items":
    st.title(f"You have selected {selected}")
    
if selected=="Contact Us":
    st.title(f"You have selected {selected}")
if selected=="Payment":
    st.title(f"You have selected {selected}")
    """